# auto-migrated from sqllogictest; review before relying on it
# 1. setup/teardown 未自动切分, 全在 steps; 必要时人工重组
# 2. 变量反向替换仅做高置信度模式 (FLOAT[N]/GRAPH_INDEX/距离函数)
# 3. 复杂指令 (loop/onlyif/skipif) 保留为 raw_directive
name: index__graph_index_metadata_dml
tags:
- vex
- migrated
engines:
- duckdb
- pg
- opengauss
description: Test DML on table with metadata GRAPH_INDEX (TAPD Bug 1077800)
steps:
- statement: CREATE TABLE t (id BIGINT, v ${VECTOR(4)});
- statement: INSERT INTO t SELECT i, ${VEC_LITERAL([(i*0.1)::FLOAT, (i*0.2)::FLOAT, (i*0.3)::FLOAT, (i*0.4)::FLOAT], 4)} FROM ${RANGE(100)} t(i);
- statement: CREATE INDEX idx ON t USING ${VEX_INDEX}(v, id${OPS_L2_COL})${IDX_OPTS_L2};
- statement: UPDATE t SET id = 2 WHERE id > 50;
- statement: DELETE FROM t WHERE id = 2;
- statement: INSERT INTO t VALUES (999, ${VEC_LITERAL([1.0, 2.0, 3.0, 4.0], 4)});
- query: SELECT count(*) FROM (SELECT id FROM t ORDER BY ${L2(v, ${VEC_LITERAL([1.0, 2.0, 3.0, 4.0], 4)})} LIMIT 5);
  _field_types: I
  expect:
  - - 5
- statement: DROP TABLE t;
_migrated_from: vexdb-duck/test/sql/vex/index/graph_index_metadata_dml.test
