# auto-migrated from sqllogictest; review before relying on it
# 1. setup/teardown 未自动切分, 全在 steps; 必要时人工重组
# 2. 变量反向替换仅做高置信度模式 (FLOAT[N]/GRAPH_INDEX/距离函数)
# 3. 复杂指令 (loop/onlyif/skipif) 保留为 raw_directive
name: index__graph_index_ddl_dup_cols
tags:
- vex
- migrated
engines:
- duckdb
- pg
- opengauss
description: Reject invalid column lists at CREATE INDEX time
steps:
- statement: CREATE TABLE t (c1 INTEGER, v ${VECTOR(4)}, v2 ${VECTOR(4)});
- statement: INSERT INTO t VALUES (1, ${VEC_LITERAL([1.0, 0.0, 0.0, 0.0], 4)}, ${VEC_LITERAL([0.0, 0.0, 0.0, 1.0], 4)});
- statement: CREATE INDEX i1 ON t USING ${VEX_INDEX}(v, c1, c1${OPS_L2_COL})${IDX_OPTS_L2};
  expect_error: duplicate column
- statement: CREATE INDEX i2 ON t USING ${VEX_INDEX}(v, v2${OPS_L2_COL})${IDX_OPTS_L2};
  expect_error: only the first column may be a vector
- statement: CREATE INDEX i3 ON t USING ${VEX_INDEX}(v, v${OPS_L2_COL})${IDX_OPTS_L2};
  expect_error: only the first column may be a vector
- statement: CREATE INDEX i4 ON t USING ${VEX_INDEX}(v, c1, v${OPS_L2_COL})${IDX_OPTS_L2};
  expect_error: only the first column may be a vector
- statement: CREATE INDEX ok_idx ON t USING ${VEX_INDEX}(v, c1${OPS_L2_COL})${IDX_OPTS_L2};
- statement: DROP TABLE t;
_migrated_from: vexdb-duck/test/sql/vex/index/graph_index_ddl_dup_cols.test
