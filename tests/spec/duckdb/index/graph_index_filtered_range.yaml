# auto-migrated from sqllogictest; review before relying on it
# 1. setup/teardown 未自动切分, 全在 steps; 必要时人工重组
# 2. 变量反向替换仅做高置信度模式 (FLOAT[N]/GRAPH_INDEX/距离函数)
# 3. 复杂指令 (loop/onlyif/skipif) 保留为 raw_directive
name: index__graph_index_filtered_range
tags:
- vex
- migrated
engines:
- duckdb
- pg
- opengauss
description: Test GRAPH_INDEX with range predicates on metadata columns (>, <, BETWEEN)
steps:
- statement: CREATE TABLE products (id INTEGER, vec ${VECTOR(4)}, price FLOAT);
- statement: "INSERT INTO products VALUES\n    (1,  ${VEC_LITERAL([1.0, 0.0, 0.0, 0.0], 4)}, 10.0),\n    (2,  ${VEC_LITERAL([0.9, 0.1, 0.0, 0.0], 4)}, 20.0),\n    (3,  ${VEC_LITERAL([0.8, 0.2, 0.0, 0.0],\
    \ 4)}, 30.0),\n    (4,  ${VEC_LITERAL([0.7, 0.3, 0.0, 0.0], 4)}, 40.0),\n    (5,  ${VEC_LITERAL([0.6, 0.4, 0.0, 0.0], 4)}, 50.0),\n    (6,  ${VEC_LITERAL([0.0, 1.0, 0.0, 0.0], 4)}, 60.0),\n    (7, \
    \ ${VEC_LITERAL([0.0, 0.9, 0.1, 0.0], 4)}, 70.0),\n    (8,  ${VEC_LITERAL([0.0, 0.8, 0.2, 0.0], 4)}, 80.0),\n    (9,  ${VEC_LITERAL([0.0, 0.0, 1.0, 0.0], 4)}, 90.0),\n    (10, ${VEC_LITERAL([0.0, 0.0,\
    \ 0.0, 1.0], 4)}, 100.0),\n    (11, ${VEC_LITERAL([0.5, 0.5, 0.0, 0.0], 4)}, 25.0),\n    (12, ${VEC_LITERAL([0.0, 0.5, 0.5, 0.0], 4)}, 55.0),\n    (13, ${VEC_LITERAL([0.0, 0.0, 0.5, 0.5], 4)}, 75.0),\n\
    \    (14, ${VEC_LITERAL([0.3, 0.3, 0.3, 0.0], 4)}, 35.0),\n    (15, ${VEC_LITERAL([0.0, 0.3, 0.3, 0.3], 4)}, 65.0);"
- statement: CREATE INDEX idx ON products USING ${VEX_INDEX} (vec, price${OPS_L2_COL})${IDX_OPTS_L2};
- query: 'SELECT id FROM products

    WHERE price = 10.0

    ORDER BY ${L2(vec, ${VEC_LITERAL([1.0, 0.0, 0.0, 0.0], 4)})}

    LIMIT 1;'
  _field_types: I
  expect:
  - - 1
- query: 'SELECT id, price::INTEGER FROM products

    WHERE price > 50

    ORDER BY ${L2(vec, ${VEC_LITERAL([0.0, 1.0, 0.0, 0.0], 4)})}

    LIMIT 2;'
  _field_types: II
  expect:
  - - 6
    - 60
  - - 7
    - 70
- query: 'SELECT id, price::INTEGER FROM products

    WHERE price < 30

    ORDER BY ${L2(vec, ${VEC_LITERAL([1.0, 0.0, 0.0, 0.0], 4)})}

    LIMIT 3;'
  _field_types: II
  expect:
  - - 1
    - 10
  - - 2
    - 20
  - - 11
    - 25
- query: 'SELECT id, price::INTEGER FROM products

    WHERE price BETWEEN 30 AND 70

    ORDER BY ${L2(vec, ${VEC_LITERAL([0.5, 0.5, 0.0, 0.0], 4)})}

    LIMIT 3;'
  _field_types: II
  expect:
  - - 5
    - 50
  - - 4
    - 40
  - - 14
    - 35
- query: 'SELECT id, price::INTEGER FROM products

    WHERE price >= 90

    ORDER BY ${L2(vec, ${VEC_LITERAL([0.0, 0.0, 0.0, 1.0], 4)})}

    LIMIT 2;'
  _field_types: II
  expect:
  - - 10
    - 100
  - - 9
    - 90
- statement: CREATE TABLE items_int (id INTEGER, vec ${VECTOR(3)}, rating INTEGER);
- statement: "INSERT INTO items_int VALUES\n    (1, ${VEC_LITERAL([1.0, 0.0, 0.0], 3)}, 1),\n    (2, ${VEC_LITERAL([0.9, 0.1, 0.0], 3)}, 2),\n    (3, ${VEC_LITERAL([0.8, 0.2, 0.0], 3)}, 3),\n    (4, ${VEC_LITERAL([0.0,\
    \ 1.0, 0.0], 3)}, 4),\n    (5, ${VEC_LITERAL([0.0, 0.9, 0.1], 3)}, 5),\n    (6, ${VEC_LITERAL([0.0, 0.0, 1.0], 3)}, 3),\n    (7, ${VEC_LITERAL([0.5, 0.5, 0.0], 3)}, 4),\n    (8, ${VEC_LITERAL([0.0,\
    \ 0.5, 0.5], 3)}, 2);"
- statement: CREATE INDEX idx_int ON items_int USING ${VEX_INDEX} (vec, rating${OPS_L2_COL})${IDX_OPTS_L2};
- query: 'SELECT id, rating FROM items_int

    WHERE rating > 3

    ORDER BY ${L2(vec, ${VEC_LITERAL([0.0, 1.0, 0.0], 3)})}

    LIMIT 2;'
  _field_types: II
  expect:
  - - 4
    - 4
  - - 5
    - 5
- query: 'SELECT id, rating FROM items_int

    WHERE rating BETWEEN 2 AND 4

    ORDER BY ${L2(vec, ${VEC_LITERAL([1.0, 0.0, 0.0], 3)})}

    LIMIT 3;'
  _field_types: II
  expect:
  - - 2
    - 2
  - - 3
    - 3
  - - 7
    - 4
- query: 'SELECT id FROM products

    WHERE price > 50

    ORDER BY ${L2(vec, ${VEC_LITERAL([0.0, 0.0, 1.0, 0.0], 4)})}

    LIMIT 1;'
  _field_types: I
  expect:
  - - 9
- query: "SELECT id FROM (\n    SELECT id, ${L2(vec, ${VEC_LITERAL([0.0, 0.0, 1.0, 0.0], 4)})} AS dist\n    FROM products\n    WHERE price > 50\n) sub\nORDER BY dist\nLIMIT 1;"
  _field_types: I
  expect:
  - - 9
- statement: DROP TABLE products;
- statement: DROP TABLE items_int;
_migrated_from: vexdb-duck/test/sql/vex/index/graph_index_filtered_range.test
